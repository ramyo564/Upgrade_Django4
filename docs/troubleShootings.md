# 트러블슈팅 통합 정리

## Backend

| 날짜 | 문서 | 문제 | 해결 | 결과 | 기능(도메인) | 한 줄 요약 |
| --- | --- | --- | --- | --- | --- | --- |
| 2023.05 ~ | `README.md` (3. 핵심 문제 해결 경험 - 보안을 고려한 인증 플로우 설계, `#lm-case-auth-security`) | 회원가입 시 이메일 소유 검증 부재와 `/admin` 노출로 유령 계정·무차별 로그인 공격 위험 | 이메일 토큰 인증(`EmailMultiAlternatives`, `urlsafe_base64_encode`)으로 계정 활성화 조건화 + 가짜 어드민(Honeypot) 페이지와 IP 기록/차단 로직 적용 | 계정 신뢰도 향상, 관리자 로그인 경로 공격 방어 강화 | 인증/보안 | 이메일 인증과 허니팟 전략을 결합해 인증 신뢰성과 관리자 보안을 동시에 확보했습니다. |
| 2023.05 ~ | `README.md` (3. 핵심 문제 해결 경험 - 복잡한 커머스 로직 구현, `#lm-case-commerce-logic`) | 비로그인 사용자가 담은 장바구니가 로그인 후 사라지거나 중복 처리될 위험 | `request.session.session_key` 기반 `cart_id` 발급, 로그인 시 세션 장바구니를 `Account` 장바구니로 병합하고 세션 키 정리 | 익명/회원 전환 시 장바구니가 끊기지 않는 사용자 경험 제공 | 장바구니/세션 | 세션-계정 병합 로직으로 로그인 전후 장바구니 일관성을 유지했습니다. |
| 2023.05 ~ | `README.md` (3. 핵심 문제 해결 경험 - 복잡한 커머스 로직 구현, `#lm-case-commerce-logic`) | `PayPal`(SDK)과 `카카오페이`(REST API)의 연동 방식 차이로 주문 처리 분산 가능성 | 결제 수단과 분리된 공통 주문 처리 로직(`place_order`)을 기준으로 결제 성공 후 동일 후처리(주문 생성/재고 차감/이메일 발송) 수행 | 결제 방식이 달라도 동일한 주문 상태 전이를 보장 | 결제/주문 | 이질적인 결제 연동을 공통 주문 서비스로 표준화해 처리 일관성을 확보했습니다. |
| 2023.05 ~ | `README.md` (2. 기능 구현 - Review, `#lm-feature-review-system`) | 비회원/미구매 사용자 리뷰 허용 시 신뢰도 저하 가능 | 로그인 여부와 구매 이력 조건을 검증해 리뷰 작성 권한 제어, 리뷰 등록 시 평균 별점/리뷰 수 동시 반영 | 리뷰 품질과 상품 평점 지표의 신뢰성 개선 | 리뷰 시스템 | 권한 검증과 집계 반영을 결합해 리뷰 데이터의 품질을 높였습니다. |

## Frontend

| 날짜 | 문서 | 문제 | 해결 | 결과 | 기능(도메인) | 한 줄 요약 |
| --- | --- | --- | --- | --- | --- | --- |
| 2023.05 ~ | `README.md` (2. 기능 구현 - Search, `#lm-feature-search`) | 키워드 검색 결과가 많을 때 사용자 탐색 피로 증가 | 상품명/설명 키워드 검색과 함께 매칭 상품 개수 카운팅을 화면에 반영 | 검색 결과 인지성이 높아져 탐색 효율 개선 | 검색 UI/UX | 검색 결과 개수 노출로 사용자가 탐색 범위를 즉시 파악할 수 있게 했습니다. |
| 2023.05 ~ | `README.md` (2. 기능 구현 - Paginator, `#lm-feature-paginator`) | 상품 목록이 길어질수록 단일 페이지 로딩/가독성 저하 | Django `Paginator` 기반 페이지 단위 조회 적용 | 목록 응답성과 가독성 향상 | 목록 UI/UX | 페이지네이션을 통해 긴 상품 목록의 탐색성과 성능 체감을 개선했습니다. |
| 2023.05 ~ | `README.md` (2. 기능 구현 - Sort by/카테고리 필터, `#lm-feature-sort-filter`, `#lm-feature-category-filter`) | 카테고리/조건별 상품 탐색이 직관적이지 않음 | 정렬(`Sort by`)과 카테고리/필터링 동작을 결합해 조건 기반 탐색 흐름 구성 | 사용자가 원하는 상품군으로 빠르게 이동 가능 | 상품 탐색 UI/UX | 정렬과 필터를 결합해 탐색 동선을 단순화했습니다. |

## DevOps

| 날짜 | 문서 | 문제 | 해결 | 결과 | 기능(도메인) | 한 줄 요약 |
| --- | --- | --- | --- | --- | --- | --- |
| 2023.05 ~ | `README.md` (3. 핵심 문제 해결 경험 - AWS 클라우드 배포 및 데이터 마이그레이션, `#lm-case-aws-migration`) | 로컬 `SQLite` 데이터를 프로덕션 `PostgreSQL`로 손실 없이 이전해야 함 | `dumpdata`로 JSON 추출 후 AWS 배포 환경에서 `loaddata`로 주입, `Elastic Beanstalk` + `RDS for PostgreSQL` 구성 | 이기종 DB 전환과 클라우드 배포를 안정적으로 완료 | 배포/데이터 마이그레이션 | Django 내장 마이그레이션 흐름으로 SQLite에서 PostgreSQL 전환을 성공시켰습니다. |
| 2023.05 ~ | `README.md` (3. 핵심 문제 해결 경험 - AWS 클라우드 배포 및 데이터 마이그레이션, `#lm-case-aws-migration`) | 애플리케이션과 DB 간 통신 노출 시 보안 리스크 존재 | `VPC` 내부 통신으로 애플리케이션 서버와 RDS 네트워크 경로를 제한 | 데이터베이스 접근 보안 강화 | 인프라 보안(AWS) | VPC 내부 통신 설계로 배포 환경의 기본 보안 수준을 높였습니다. |
